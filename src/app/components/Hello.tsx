import * as React from 'react';
interface IProps {
  
}
export default class Hello extends React.Component<IProps, {}> {
   render() {
   return <h1>This is a  application using react and redux app   </h1>
   }
}